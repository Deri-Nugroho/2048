apiVersion: v1
kind: Service
metadata:
  name: workshop-app
  labels:
    app: workshop-app
spec:
  type: ClusterIP
  selector:
    app: workshop-app
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
